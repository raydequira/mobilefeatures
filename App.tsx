import React, { Component } from 'react'
import { View, StyleSheet, ScrollView } from 'react-native'

import Navigator from './routing/Nav'

const App = () => {
    return (
        <Navigator/>
    )
 }
 export default App
